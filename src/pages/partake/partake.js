import Taro, { Component } from '@tarojs/taro'
import { View } from '@tarojs/components'

export default class Partake extends Component {

  config = {
    navigationBarTitleText: '最近参与'
  }

  render () {
    return (
      <View>哈有</View>
    )
  }
}